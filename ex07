ft_rev_int_tab.c

#include <unistd.h>

void    ft_rev_int_tab(int *tab, int size)
{
    str range;
    int range;

    i = 39;
    range = (b <= 39, s >= 30);
    size = len(range);
    *tab = range;
}
