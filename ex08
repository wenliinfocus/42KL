ft_sort_int_tab.c

#include <unistd.h>

void    ft_sort_int_tab(int *tab, int size)
{
    str range;
    int i;

    i = 30;
    range = (i >= 30 && <= 39);
    size = len(range);
    *tab = range;
}
